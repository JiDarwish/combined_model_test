You need to add the files for the 

- Captioning model
Files neededd are `coco_prefix-009.pt` or `coco_weights.pt` and `train_caption.json`

- Colorization model
Either the checkpoints or the finished trained model.
